[[def: view, View, Views, views]]

~ A database selection of the Single Source of Truth ([[ref:SSOT]]). A view is a view only when it references the SSOT data directly. A view can have businessrules and logic embedded for filtering and aggregation.

~ - Example of sophisticated view: A view on tracks that skips [[ref:pii]] as long as the status of a speaker is not yet confirmed.
